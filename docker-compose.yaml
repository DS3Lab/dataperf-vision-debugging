services:
  dataperf-debugging-submission:
    container_name: dataperf-debugging-submission
    build: .
    volumes:
      - ./data:/app/data
      - ./embeddings:/app/embeddings
      - ./results:/app/results
      - ./submissions:/app/submissions